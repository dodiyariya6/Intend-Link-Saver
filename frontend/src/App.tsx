/**
 * Root app component — scaffolding only.
 *
 * No routing/pages/auth are wired up yet. This just confirms the frontend
 * boots and can be pointed at the FastAPI backend's /health endpoint later.
 */
function App() {
  return (
    <div style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>Intend Link Saver</h1>
      <p>Frontend scaffold is running. No pages implemented yet.</p>
    </div>
  );
}

export default App;
